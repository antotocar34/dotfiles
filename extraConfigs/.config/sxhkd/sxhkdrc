alt + w
    /home/carneca/Documents/Scripts/find_tex.sh

alt + f
    kitty --name vifm -e vifm .

shift + alt +t kitty --name tmux tmux a

alt + r  
    /home/carneca/Documents/Scripts/find_pdf.sh

alt + c 
    rofi -show run

alt + Return 
    rofi -show drun -show-icons

alt + v 
   kitty -e  vim

alt + n
   bitwarden 

alt + m
   tdrop -n=1 -ma -w -4 -y "$PANEL_HEIGHT" kitty --name cmus-term -e cmus

ctrl + Return
    kitty --name term0 -d ~ env TDROP= bash

F12
    tdrop --number=421 -ma -w -4 -y "50%" kitty --name term1 -d ~ env TDROP="red" bash

ctrl + numbersign 
    tdrop --number=420 -ma -w -4 kitty --name term2 -d ~ env TDROP="purple" bash

ctrl + b
    chromium --new-window https://www.google.com

shift + ctrl + g
    tdrop --number=google -ma chromium --new-window https://www.google.com

shift + ctrl + m
    tdrop --number=messenger -ma chromium --new-window https://www.messenger.com/t/100009009790305/

alt + shift + b
    tdrop --number=books -ma chromium --new-window https://1lib.eu/

alt + shift + slash
    tdrop --number=articles -ma chromium --new-window https://booksc.xyz/

alt + s
    anki

alt + e
    rofi -show window

Print
    flameshot gui

alt + shift + r
    pkill -usr1 -x sxhkd ; notify-send --expire-time=2000 'sxhkd' 'Reloaded config'

alt + shift + p
    bw generate -p --words 5 | xclip -selection clipboard ; notify-send --expire-time=2000 'Generated password'
