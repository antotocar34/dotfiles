input_full=$(readlink -f $1)
#
# /home/carneca/Documents/Latex -> Documents/Latex
#
truncatePath () {
echo ${input_full} | sed 's#.*\(Documents\)#\1#g'
    }

dpath=$(truncatePath $1)

rclone_command () {
    rclone copy -vP tdrive:/backup/${1}/ ${input_full}/
    }

rclone_command $dpath
